<div class="can-scroll">
	<div class="col-md-12">
		<% @artists.each do |artist| %>
		<a href="<%= artist_path(artist) %>" class="">
			<div class="col-md-4b artistContainer">
				<div class="artist">
					<div class="info-wrapper">
						<h5 class="colour"><%= artist.name %></h5>
						<hr class="dots"/>
						<p class="artistDescription"><%= truncate(artist.description, :length => 220, :separator => ' ') %></p>
					</div>
					<%= image_tag(artist.image, lazy: true, class: "lazy") %>
				</div>
			</div>
		</a>
		<% end %>
	</div>
</div>